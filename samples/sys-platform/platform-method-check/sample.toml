id = "platform-method-check"
feature = "startswith"
description = "Method-based platform checks are a contrast case for post-conditional narrowing"
expectation = "accept"
tags = ["sys.platform", "conditional", "startswith"]
